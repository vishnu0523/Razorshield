.env
*.db
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
node_modules/
frontend/tsconfig.tsbuildinfo
data/*.parquet
data/*.csv
data/events/
data/labels/
data/features.parquet
.claude/

# artifacts/ and frontend/dist/ are intentionally tracked, not ignored.
#
# This repo ships a working snapshot: a fresh clone runs immediately with
# real numbers (no ML pipeline run, no `npm install`, one process, one
# command) instead of showing an untrained placeholder or a broken screen
# until someone finds and runs the right setup commands. `make reproduce`
# still regenerates every one of these files byte-for-byte from seed 42 --
# shipping the snapshot does not weaken that claim, it just means nobody has
# to run the regeneration before they're allowed to look at the product.
#
# razorshield.db is deliberately NOT in that list. Every server start already
# calls seed() (backend/app/core/seed.py), which is idempotent -- it creates
# the tables and replays the audit trail from artifacts/rings.json only when
# the log is empty, and is a no-op otherwise. A committed db bought nothing
# a fresh clone didn't already get for free, and cost a manual reset before
# every commit plus Windows file-locking whenever a running server held it
# open. Never commit it again.
